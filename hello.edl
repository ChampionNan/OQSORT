enclave {
  from "openenclave/edl/syscall.edl" import *;
  from "openenclave/edl/sgx/platform.edl" import *;

  trusted {
    public oe_result_t enclave_hello(
      [in, count=262144] int* arg1,
      );
  };

  untrusted {
    oe_result_t host_hello(
      int* arg2
      );
  };
};